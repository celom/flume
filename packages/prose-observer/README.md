# prose-observer

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build prose-observer` to build the library.

## Running unit tests

Run `nx test prose-observer` to execute the unit tests via [Vitest](https://vitest.dev/).
